{
  "name": "medibook",
  "version": "1.0.0",
  "description": "MediBook — Doctor Appointment Booking (Frontend Only)",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build"
  }
}
